type Restaurant @model @auth(rules: [{ allow: public, provider: apiKey, operations: [read] }, { allow: groups, groups: ["Admin"] }]) {
  id: ID!
  name: String!
  address: String
  category: String!
  website: String!
  state: String
  city: String
  zipcode: String
  rating: Int
  items: [String]
  profilePic: String
  reviews: [Review] @hasMany
}

type User @model @auth(rules: [{ allow: owner }, { allow: groups, groups: ["Admin"] }]) {
  id: ID!
  email: String!
  reviews: [Review] @hasMany
}

type Review @model @auth(rules: [{ allow: owner }, { allow: groups, groups: ["Admin"] }]) {
  id: ID!
  poster: String!
  rating: Int!
  message: String
  photos: [String]
  approved: Boolean!
}
